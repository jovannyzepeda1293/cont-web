# 🚀 React Project

This is a project built with **React + Vite**.

## 📦 Installation

Clone the repository and install dependencies:

```bash
git clone repo path
cd repo name
npm install
```

## Environment Variables

Copy the env example file and update values as needed:

```bash
cp .env.example .env
```

## Development

Start the development server with:
```bash
npm run dev
```
